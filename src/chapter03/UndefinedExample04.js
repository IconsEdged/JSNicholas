var message; //declared variable;
//undeclared variable
//var age;

alert("typeof of an declared but unintialized variable: = " +typeof message);
alert("type of an undeclared variable: = " +typeof age);
/* Since it is very difficult to seperate the undeclared variable from declared variable we can try to 
 * make it a rule that all the declared variable should have a intilized value.
 */

