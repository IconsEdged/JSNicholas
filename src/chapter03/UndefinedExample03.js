var message;    //this variable is declared but has no value.
//below variable should be undeclared.
//var age;
alert("declared variable value = " +message);
alert("only one operation can be done one an undeclared variable: " +typeof age);
alert("undeclared variable value = "+age);
